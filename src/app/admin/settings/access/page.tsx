import SettingsWorkspace from '../SettingsWorkspace';

// /admin/settings/access — Access Control (users, roles, permissions, sessions)
export default function AccessControlPage() {
  return <SettingsWorkspace group="access" />;
}
