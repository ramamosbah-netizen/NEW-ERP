import SettingsWorkspace from '../SettingsWorkspace';

// /admin/settings/operational — Operational Scales (finance, procurement, inventory, projects, maintenance, HR)
export default function OperationalScalesPage() {
  return <SettingsWorkspace group="operational" />;
}
